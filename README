README

Instructions to get running locally:

1. Server
cd to foood directory
php -S localhost:8000

2. JSX Compile
cd to foood scripts directory
babel --presets react --watch src/ --out-dir lib/

3. Database
mysql
